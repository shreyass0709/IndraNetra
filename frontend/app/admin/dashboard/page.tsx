import { Placeholder } from '../../../components/layout/Placeholder';
import { copy } from '../../../lib/copy';

// Real KPIs + live map arrive in Phase 4.
export default function AdminDashboard() {
  return <Placeholder title={copy.nav.dashboard} />;
}
